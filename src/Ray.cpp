#include "Ray.h"
#include "Triangle.h"
//
//
//Vec3f Intersection::brdf(Vec3f const &wi, Vec3f const &wo)
//{
//    Vec2f uv = bary[0] * tri->t[0] + bary[1] * tri->t[1] + bary[2] * tri->t[2];
//    auto albedo = material->kd_map->get(uv.x, uv.y);
//    return albedo / static_cast<float>(PI);
//}
